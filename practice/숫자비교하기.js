{
  // 풀이 (1)
  const solution = (num1,num2) => {
    if (num1 === num2) {
      return 1
    }
    else {
      return -1
    }
  }
}

{
  // 풀이 (2)
  const solution = (num1,num2) => {
    var answer = num1 === num2 ? 1 : -1;
    return answer
  };
  console.log(solution(num1=1,num2=1))
}